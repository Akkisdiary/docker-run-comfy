# =============================================================================
# Docker Ignore File for ComfyUI WAN Template
# =============================================================================
# Purpose: Exclude unnecessary files from Docker build context
# Benefits: Faster builds, smaller context, improved security
# =============================================================================

# =============================================================================
# VERSION CONTROL AND DEVELOPMENT FILES
# =============================================================================
# Exclude version control and development-specific files
.git/                    # Git repository data (not needed in container)
.gitignore              # Git ignore rules (not needed in container)
.github/                # GitHub workflows and templates
.vscode/                # VS Code settings and extensions
.idea/                  # IntelliJ IDEA project files
*.swp                   # Vim swap files
*.swo                   # Vim swap files
*~                      # Backup files

# =============================================================================
# DOCUMENTATION AND README FILES
# =============================================================================
# Keep essential docs but exclude development documentation
*.md                    # Markdown files (README, docs, etc.)
docs/                   # Documentation directory
examples/               # Example files and tutorials

# =============================================================================
# TEMPORARY AND CACHE FILES
# =============================================================================
# Exclude all temporary and cache files to reduce build size
__pycache__/            # Python bytecode cache
*.pyc                   # Python compiled files
*.pyo                   # Python optimized files
*.pyd                   # Python extension modules
.pytest_cache/          # Pytest cache directory
.coverage               # Coverage reports
htmlcov/                # HTML coverage reports
.tox/                   # Tox testing environments
.cache/                 # General cache directory
*.log                   # Log files
logs/                   # Log directories

# =============================================================================
# OPERATING SYSTEM FILES
# =============================================================================
# Exclude OS-specific files that shouldn't be in containers
.DS_Store               # macOS directory metadata
.DS_Store?              # macOS directory metadata variants
._*                     # macOS resource forks
.Spotlight-V100         # macOS Spotlight index
.Trashes                # macOS trash metadata
ehthumbs.db             # Windows thumbnail cache
Thumbs.db               # Windows thumbnail database
Desktop.ini             # Windows desktop configuration

# =============================================================================
# MODEL AND DATA FILES
# =============================================================================
# Exclude large model files from build context (downloaded at runtime)
models/                 # Model files (too large for build context)
*.ckpt                  # Checkpoint files
*.safetensors           # SafeTensors model files
*.bin                   # Binary model files
*.pth                   # PyTorch model files
*.pt                    # PyTorch tensor files
outputs/                # Generated outputs
input/                  # Input files
cache/                  # Model cache directories

# =============================================================================
# ENVIRONMENT AND CONFIGURATION
# =============================================================================
# Exclude environment-specific configuration files and sensitive data
.env                    # Environment variables file (contains tokens)
.env.*                  # All environment files
!.env.example           # Exception: include the example template
.env.local              # Local environment overrides
.env.*.local            # Environment-specific local files
config.local.*          # Local configuration overrides
secrets/                # Secrets directory
*.key                   # Key files
*.pem                   # Certificate files
*.crt                   # Certificate files

# =============================================================================
# BUILD AND DISTRIBUTION FILES
# =============================================================================
# Exclude build artifacts and distribution files
build/                  # Build output directory
dist/                   # Distribution files
*.egg-info/             # Python package metadata
.eggs/                  # Python eggs
wheels/                 # Python wheel files
*.whl                   # Python wheel packages

# =============================================================================
# TESTING AND QUALITY ASSURANCE
# =============================================================================
# Exclude testing and QA related files
tests/                  # Test files and directories
test_*.py               # Test scripts
*_test.py               # Test scripts
.pytest_cache/          # Pytest cache
.mypy_cache/            # MyPy type checker cache
.bandit                 # Bandit security scanner cache
.flake8                 # Flake8 linter cache

# =============================================================================
# EDITOR AND IDE SPECIFIC FILES
# =============================================================================
# Exclude editor-specific configuration and temporary files
.vscode/                # Visual Studio Code settings
.idea/                  # JetBrains IDE settings
*.sublime-project       # Sublime Text project files
*.sublime-workspace     # Sublime Text workspace files
.atom/                  # Atom editor settings
.brackets.json          # Brackets editor settings

# =============================================================================
# RUNTIME AND DEPLOYMENT FILES
# =============================================================================
# Exclude files that are created at runtime or deployment-specific
node_modules/           # Node.js dependencies (if any)
npm-debug.log*          # NPM debug logs
yarn-debug.log*        # Yarn debug logs
yarn-error.log*        # Yarn error logs
.npm                    # NPM cache
.yarn-integrity         # Yarn integrity file

# =============================================================================
# CUSTOM EXCLUSIONS
# =============================================================================
# Project-specific files to exclude
*.backup                # Backup files
*.orig                  # Original files from merges
*.rej                   # Rejected patches
# KEEP ESSENTIAL FILES
# =============================================================================
# Explicitly include essential files (overrides above exclusions)
!Dockerfile             # Always include Dockerfile
!requirements.txt       # Always include Python requirements
!requirements.m1.txt    # Always include M1 Mac requirements

# M1 Mac testing files (not needed in production image)
docker-compose.m1.yml     # M1 Mac testing compose file
Dockerfile.m1             # M1 Mac testing Dockerfile
workspace/                # M1 Mac development volumes
cache/                    # M1 Mac development volumes